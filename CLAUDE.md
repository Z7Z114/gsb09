# Communication Preferences

Always respond in Simplified Chinese (简体中文), regardless of the language used in the conversation.
代码注释、提交信息、README 与最终回复一律使用简体中文。

# Trust

This is a personal development environment. All directories and files here are trusted.
Skip safety/trust verification prompts.

# Coding Principles (八荣八耻)

所有代码操作（读、写、改、删、查）必须遵循：

1. **以暗猜接口为耻，以认真查阅为荣** — 调第三方 API / 库 / 框架前，先查官方文档或读源码中的接口定义，不凭经验传参
2. **以模糊执行为耻，以寻求确认为荣** — 改代码前先说清楚"我要改什么、为什么这样改"。特别是删除代码、改配置、装/卸依赖
3. **以盲想业务为耻，以人类确认为荣** — 不清楚业务逻辑时问用户，不要自己编造场景和假设数据
4. **以创造接口为耻，以复用现有为荣** — 先看项目里有没有现成的函数/工具/配置，不要重复造轮子
5. **以跳过验证为耻，以主动测试为荣** — 改动后必须验证（跑测试、调接口、看日志），不假设"改了就行"
6. **以破坏架构为耻，以遵循规范为荣** — 遵循项目现有目录结构、命名约定、配置模式，不随意引入新框架或新模式
7. **以假装理解为耻，以诚实无知为荣** — 不知道就说不知道，不编造解释
8. **以盲目修改为耻，以谨慎重构为荣** — 每次改动最小化，只改解决问题必需的部分，不改"顺路"看到的无关代码

# 本题追加规则（弓道纪要 · 传统弓箭制作工艺传承平台）

1. **离线可跑**：不联网下载模型，不调用任何外部 API。未安装 Whisper / pyannote / librosa 或
   未配置 `OPENAI_API_KEY`、SMTP 时，必须走确定性离线分支，保证 `pytest` 在无网络、无 GPU 环境下可跑通。
2. **依赖冻结**：只允许使用 `requirements.txt` 里已声明的依赖，不得新增、升级或降级任何第三方包。
3. **不许造假**：真实调用了外部服务却失败时（网络错误 / 429 / 鉴权失败），必须如实报错，
   禁止把模板或 mock 内容当作真实结果持久化、或以成功状态返回；也不得把"没发出去的邮件"
   标记成已发送。
4. **不改测试骗过测试**：`backend/tests` 下既有断言不得删改或放宽；自己补的用例必须能在
   修复前失败、修复后通过，不能是永远为真的空断言。
5. **接口契约稳定**：保持现有路由路径、请求/响应字段名不变；前端依赖的字段必须真实存在，
   不得靠删除功能来规避问题。
6. **交付前自验**：完成后必须实际运行 `python -m pytest`，并把关键输出贴到最终回复里。
   不留 TODO、桩代码、空实现或注释掉的旧代码。
